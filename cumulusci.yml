minimum_cumulusci_version: "3.78.0"
project:
    name: Shluman Notes App
    package:
        name: ShlumanNotesApp
        name_managed: Shluman Notes App
        api_version: '64.0'
    git:
        default_branch: 'main'
        prefix_feature: 'feature/'
        prefix_beta: 'beta/'
        prefix_release: 'release/'
    source_format: sfdx
    dependencies:
        - github: https://github.com/Nimba-Solutions/Shulman-Core.git

tasks:
    robot:
        options:
            suites: robot/Shluman-Notes-App/tests
            options:
                outputdir: robot/Shluman-Notes-App/results

    robot_testdoc:
        options:
            path: robot/Shluman-Notes-App/tests
            output: robot/Shluman-Notes-App/doc/Shluman-Notes-App_tests.html

    run_tests:
        options:
            required_org_code_coverage_percent: 75

    # CumulusCI ships with both `retrieve_changes`, `permsets` and `deploy` tasks out of the box, '
    # but they don't provide sufficient find/replace functionality so this project
    # ships with custom wrapper tasks:
    # └── 📁 tasks/
    #     ├── 📄 retrieve_changes.py
    #     ├── 📄 permsets.py
    #     └── 📄 deploy.py
    retrieve_changes:
        class_path: tasks.retrieve_changes.RetrieveChanges
        options:
            preserve_tokens: "__PROJECT_NAME__,__PROJECT_LABEL__"
    deploy:
        class_path: tasks.deploy.Deploy
        options:
            transforms:
                - transform: find_replace
                  options:
                      patterns:
                          - find: "__PROJECT_NAME__"
                            replace: $project_config.project__package__name
                          - find: "__PROJECT_LABEL__"
                            replace: $project_config.project__package__name_managed

    assign_permission_set_groups:
        class_path: tasks.permsets.AssignPermissionSetsWithFindReplace
        options:
            api_names: __PROJECT_NAME__Admin
            transforms:
                - transform: find_replace
                  options:
                      patterns:
                          - find: "__PROJECT_NAME__"
                            replace: $project_config.project__package__name

flows:
    config_dev:
        steps:
            3.0:
                task: assign_permission_set_groups

    config_qa:
        steps:
            3.0:
                task: assign_permission_set_groups
